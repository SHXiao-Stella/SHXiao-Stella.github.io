source 'https://rubygems.org'

# Keep local builds aligned with the GitHub Pages-compatible stack.
gem 'github-pages'
gem 'connection_pool', '2.5.0'
gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw]

group :jekyll_plugins do
  gem 'jekyll'
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jekyll-redirect-from'
  gem 'jemoji'
  gem 'webrick', '~> 1.8'
  gem 'hawkins'
end
